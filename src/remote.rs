pub fn search(programs: &[String]) {
    println!("This is search! I receive {:?}", programs)
}

pub fn import_remote(programs: &[String]) {
    println!("This is import remote! I receive {:?}", programs)
}